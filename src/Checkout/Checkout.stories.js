import React from 'react';
import { Checkout } from './Checkout'; // Import your component correctly

export default {
  title: 'Components/Checkout', // Adjust the title as needed
  component: Checkout,
};

export const Default = () => <Checkout />;
